# Regras do Projeto

- **Não realizar push no Git sem permissão explícita:** Nunca execute comandos de push (`git push`, etc.) para o repositório remoto sem que o usuário peça ou autorize explicitamente.
